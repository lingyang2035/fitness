# Fitness 项目 HTTPS 部署方案

## 背景

- **Fitness 应用**：Flask 单体应用，部署在容器 `9285`，当前通过 `http://xx.xxxxx.com:9101/fitness/login` 访问
- **Hiking 应用**：已通过 nginx 容器在 `https://xx.xxxxx.com:9078/` 提供服务
- **目标**：Fitness 也通过 `https://xx.xxxxx.com:9078` 访问，且两个项目互不干扰

## 核心问题

Fitness 是一个 **Flask 单体应用**，所有路径硬编码为以 `/` 开头的绝对路径（共 19 个文件），没有基础路径配置机制。

| 类别 | 硬编码示例 | 涉及文件数 |
|------|-----------|-----------|
| HTML 模板 | `href="/app"`, `src="/static/js/api.js"` | 6 |
| JavaScript | `window.location.href = '/login'`, `fetch('/api/auth/login')` | 3 |
| Service Worker | `'/static/js/api.js'`, `'/static/offline.html'` | 1 |
| PWA Manifest | `"start_url": "/app"`, `"src": "/static/icon-192.png"` | 1 |
| Flask 路由 | `@main_bp.route('/app')`, `@auth_bp.route('/api/auth/login')` | 6 |

如果强行通过 nginx 在 `/fitness/` 子路径下代理，不改代码的话：
- nginx 只能重写 HTTP 请求/响应头中的路径
- JavaScript 中 `window.location.href = '/app'` 会跳转到 `https://xx.xxxxx.com:9078/app`（404）
- JS 中 `fetch('/api/auth/login')` 会请求到 `https://xx.xxxxx.com:9078/api/auth/login`（404）
- Service Worker 注册路径和缓存路径全部失效

**结论：仅修改 nginx 配置无法解决子路径部署问题。**

---

## 方案对比

| 维度 | 方案 A：子域名 | 方案 B：子路径 + 代码改造 | 方案 C：nginx sub_filter |
|------|-------------|------------------------|------------------------|
| Fitness 代码改动 | **零** | 约 19 个文件 | JS 文件仍需改动 |
| nginx 配置复杂度 | 低 | 中 | 高 |
| 可靠性 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |
| DNS 依赖 | 需要新增记录 | 不需要 | 不需要 |
| 后续维护成本 | 极低 | 中 | 高（补丁式） |
| 两个项目隔离度 | 完全隔离 | 共享路径空间 | 共享路径空间 |

---

## 方案 A：子域名（推荐）

### 原理

nginx 通过 HTTP `Host` 头区分请求目标，同一端口（9078）同时服务两个域名：

```
https://fitness.xx.xxxxx.com:9078/  →  Fitness 容器
https://xx.xxxxx.com:9078/          →  Hiking 项目（不变）
```

### nginx 配置

```nginx
# ============================================
# Fitness 应用
# ============================================
server {
    listen 9078 ssl;
    server_name fitness.xx.xxxxx.com;

    ssl_certificate     /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    # 代理到 Fitness 容器
    location / {
        proxy_pass http://<容器9285的IP>:9101;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================
# Hiking 项目（现有配置，不变）
# ============================================
server {
    listen 9078 ssl;
    server_name xx.xxxxx.com;

    ssl_certificate     /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location / {
        # ... 现有 Hiking 配置保持不变 ...
    }
}
```

### 前提条件

- **DNS**：新增一条记录，将 `fitness.xx.xxxxx.com` 解析到宿主机 IP
- 如果 `xx.xxxxx.com` 已有泛域名解析（`*.xx.xxxxx.com`），则无需额外操作

### 优势

1. **Fitness 代码一行不改**，零风险
2. 两个项目完全隔离，cookie、session、Service Worker、PWA 互不干扰
3. nginx 配置清晰，后续维护简单
4. 未来任何一方项目变更，不影响另一方

---

## 方案 B：子路径 + 代码改造

### 原理

给 Fitness 应用添加一个可配置的路径前缀（如 `/fitness`），使其所有路由、静态资源、API 请求、JS 跳转都带上这个前缀。

### 需要改动的内容

#### 1. 后端 Flask 路由（6 个文件）

使用蓝图 `url_prefix` 或在 `create_app` 时通过中间件添加前缀：

```python
# app/__init__.py
from flask import Flask
from werkzeug.middleware.dispatcher import DispatcherMiddleware

def create_app():
    app = Flask(__name__)
    # ... 注册蓝图 ...
    return app

# 通过环境变量控制前缀
PREFIX = os.environ.get('APP_PREFIX', '')

# 在蓝图注册时添加前缀
app.register_blueprint(main_bp, url_prefix=PREFIX)
app.register_blueprint(auth_bp, url_prefix=PREFIX)
# ...
```

或者更简洁的方式，使用 `SCRIPT_NAME` 环境变量：

```python
# app/__init__.py
from werkzeug.middleware.proxy_fix import ProxyFix

app.wsgi_app = ProxyFix(app.wsgi_app, x_prefix=1)

# nginx 设置 X-Forwarded-Prefix 头即可
```

#### 2. HTML 模板（6 个文件）

使用 Jinja2 变量统一前缀：

```html
<!-- base.html -->
<head>
    <base href="{{ PREFIX }}/">
    <link rel="manifest" href="{{ PREFIX }}/static/manifest.json">
</head>
```

然后将所有绝对路径改为使用 `{{ PREFIX }}` 变量或 `url_for()`。

#### 3. JavaScript 文件（3 个文件）

在 `base.html` 中注入全局配置：

```html
<script>
    window.APP_PREFIX = "{{ PREFIX }}";
</script>
<script src="{{ PREFIX }}/static/js/api.js"></script>
```

`api.js` 中使用：

```javascript
const API = {
    BASE: window.APP_PREFIX || '',
    // ...
    login(data) {
        return fetch(this.BASE + '/api/auth/login', {
            method: 'POST',
            // ...
        });
    }
};
```

所有 `window.location.href = '/xxx'` 改为 `window.location.href = window.APP_PREFIX + '/xxx'`。

#### 4. Service Worker 和 Manifest

`sw.js` 和 `manifest.json` 也需要动态生成或使用变量替换。

### nginx 配置

```nginx
server {
    listen 9078 ssl;
    server_name xx.xxxxx.com;

    ssl_certificate     /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    # Hiking 项目
    location / {
        # ... 现有配置 ...
    }

    # Fitness 项目（子路径）
    location /fitness/ {
        proxy_pass http://<容器9285的IP>:9101/fitness/;
        # 或者代理到根路径，让应用自己处理前缀：
        # proxy_pass http://<容器9285的IP>:9101;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Prefix /fitness;
        proxy_cookie_path / /fitness/;
    }
}
```

### 优势

- 不需要新增 DNS 记录
- URL 结构统一（都在同一域名下）

### 劣势

- 改动量大（19 个文件），容易遗漏
- 后续新增功能必须记得加前缀，维护成本高
- Service Worker 在子路径下需要额外处理 scope
- PWA manifest 的 `start_url` 和 icon 路径需要动态化

---

## 方案 C：nginx sub_filter（不推荐）

### 原理

利用 nginx 的 `ngx_http_sub_module` 在返回 HTML 时动态替换路径字符串。

### 问题

- `sub_filter` 只能处理 HTML 文本替换，无法修改 JavaScript 中的运行时跳转
- `window.location.href = '/app'` 这种代码 `sub_filter` 无能为力
- 容易出现替换不完整或误替换的情况
- 调试困难，属于"黑盒补丁"

**不推荐此方案。**

---

## 建议

**推荐方案 A（子域名）**，理由：

1. Fitness 代码零改动，零风险
2. nginx 配置简单，仅在现有基础上增加一个 `server` 块
3. 两个项目完全隔离，不会互相影响
4. 后续任何一方变更都不影响另一方

唯一需要 SRE 配合的是 DNS 记录。如果 `xx.xxxxx.com` 已有 `*.xx.xxxxx.com` 泛域名解析，则 DNS 也不需要改。

---

## 待确认事项

请 SRE 确认以下问题：

1. 是否可以在 DNS 中新增 `fitness.xx.xxxxx.com` 记录指向宿主机 IP？
2. 或者 `xx.xxxxx.com` 是否已有泛域名解析（`*.xx.xxxxx.com`）？
3. 如果子域名方案不可行，是否可以接受方案 B 的代码改造成本（约 19 个文件）？
